   <div class="wrapper">
    <header class="header">
    <%# 背景画像に載せる %>
    <div class='title-contents'>
      <%= image_tag "header.png", class:"header-pict"%>
      <%# <div class="header__bar row"> %>
        <%# <h1 class="grid-6"><a href="/">GOHAN TIME</a></h1> %>
        <div class="top">
          <% if user_signed_in? %>
           
          <% else %>
            <div class="not-login">
              <%= link_to "ログイン", new_user_session_path, class: "post" %>
              <%= link_to "新規登録", new_user_registration_path, class: "post" %>
            </div>
          <% end %>
        </div>
      <%# </div> %>
    </div>
    <%## 背景画像に載せる %>
   </header>
